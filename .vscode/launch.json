{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node",
      "request": "launch",
      "name": "Run TypeScript file",
      "program": "${workspaceFolder}/${relativeFile}",
      "runtimeArgs": ["-r", "ts-node/register"],
      "cwd": "${workspaceFolder}"
    }
  ]
}
